
##Cisco

# - name: create vlans on cisco ios

### Feedback
- name: create vlans on cisco ios
  cisco.ios.ios_vlans:
    config:
      - name: vlan2
        vlan_id: 2
        state: overridden
    state: merged
  tags:
    - always
  register: _vlans_output

### Expected

- name: create cisco ios vlans
  vars:
    vlans:
    - name: VLAN100
      vlan_id: 100
      state: active
  cisco.ios.ios_vlans:
    config: "{{ vlans }}"
    state: merged


##Juniper

# - name: create vlans on cisco ios

### Feedback
- name: create vlans on cisco ios

### Expected


